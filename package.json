{
  "name": "negotiation-app",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "kill-port 3000 && node server.js",
    "dev": "kill-port 3000 && NODE_ENV=development node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "node-fetch": "^3.3.2",
    "socket.io": "^4.8.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
